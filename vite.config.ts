import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react' // для React проектов

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()], // для React проектов
  resolve: {
    
  },
})